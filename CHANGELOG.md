# Changelog

## Unreleased

- Add public repository governance policy, SpecKit public spec validation, and release package boundary checks.
- Harden V0 public scoring with runtime-only default scope, unsupported-language `not evaluated` status, preview/advisory finding separation, scoring dedupe, and rule-level score caps.
- Update Markdown, SARIF, badge, and CLI output to distinguish scored findings from preview/advisory inventory.
